@model IEnumerable<ProjetoFinalIsabella.Models.TipoColaborador>

@{
    ViewData["Title"] = "Tipo de Colaborador";
}

<h1>Tipo de Colaborador</h1>

<p>
    <a asp-action="Create" class="btn-primary">Cadastrar Tipo de Colaborador</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoColaboradorNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoColaboradorNome)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.TipoColaboradorId" class="btn-primary"><i class="fa-solid fa-pen-to-square"></i></a>
                    <a asp-action="Details" asp-route-id="@item.TipoColaboradorId" class="btn-primary"><i class="fa-solid fa-circle-info"></i></a>
                    <a asp-action="Delete" asp-route-id="@item.TipoColaboradorId" class="btn-segundo"><i class="fa-solid fa-trash-can"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
